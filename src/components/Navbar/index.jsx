import './style.css';

import Logo from '../Logo';

const Navbar = () => {
    return (
        <div className="navbar-container">
            <Logo />
        </div>
    );
}

export default Navbar;
